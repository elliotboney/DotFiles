# When you ssh, creates a new screen or reconnects to any
# open screen to save your session in case you get disconnected
# if [ -n "$SSH_CONNECTION" ] && [ -z "$SCREEN_EXIST" ]; then
#     export SCREEN_EXIST=1
#     screen -DRi
# fi
